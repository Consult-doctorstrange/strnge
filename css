body{
    color: white;
      font-family: arial, helvetica, sans-serif
    background: url(./img/2doctorstrange.jpg) no-repeat;
    margin: 0;
    padding: 0;
    background-size: cover;
}

    .box {
        width: 450px; /* Set the desired width */
        background-color: rgba(141, 14, 14, 0.612); /* Transparent black background (adjust the alpha value to change transparency) */
        backdrop-filter: blur(5px); /* Apply a blur effect to the background */
        padding: 40px;
        text-align: center;
        margin: auto;
        margin-top: 5%;
        border-radius: 0.1px;
    }
